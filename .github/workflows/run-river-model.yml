name: Run river model

on:
  workflow_dispatch:
    inputs:
      mode:
        description: What to run
        required: true
        default: smoke
        type: choice
        options:
          - smoke
          - discover
          - thresholds
          - download
          - train
          - run-all
          - status
      years:
        description: Years of USGS data for download/train/run-all
        required: true
        default: '15'
      limit:
        description: Only first N sites; 0 means all sites
        required: true
        default: '1'
      event_set:
        description: Event set to train
        required: true
        default: flood
        type: choice
        options:
          - flood
          - moderate
          - major
          - all
          - custom
      min_crest_stage:
        description: Optional explicit minimum crest stage; blank uses event_set threshold columns
        required: false
        default: ''
      force_download:
        description: Re-download even if raw CSV exists in the runner
        required: true
        default: false
        type: boolean

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run selected mode
        env:
          MODE: ${{ inputs.mode }}
          YEARS: ${{ inputs.years }}
          LIMIT: ${{ inputs.limit }}
          EVENT_SET: ${{ inputs.event_set }}
          MIN_CREST_STAGE: ${{ inputs.min_crest_stage }}
          FORCE_DOWNLOAD: ${{ inputs.force_download }}
        run: |
          python - <<'PY'
          import os
          import subprocess

          mode = os.environ['MODE']
          years = int(os.environ['YEARS'])
          limit = int(os.environ['LIMIT'])
          event_set = os.environ['EVENT_SET']
          min_crest_stage = os.environ.get('MIN_CREST_STAGE', '').strip()
          force = os.environ['FORCE_DOWNLOAD'].lower() == 'true'

          if years < 1 or years > 30:
              raise SystemExit('years must be between 1 and 30')
          if limit < 0 or limit > 100:
              raise SystemExit('limit must be between 0 and 100')

          def run(cmd):
              print('RUN:', ' '.join(str(x) for x in cmd), flush=True)
              subprocess.run([str(x) for x in cmd], check=True)

          def with_limit(cmd):
              if limit:
                  return cmd + ['--limit', limit]
              return cmd

          force_arg = ['--force'] if force else ['--skip-existing']
          crest_arg = ['--min-crest-stage', min_crest_stage] if min_crest_stage else []

          discover = ['python', 'src/nwps_multigage_model.py', 'discover-sites', '--sites', 'config/sites.csv', '--output', 'config/sites_with_usgs.csv']
          thresholds = ['python', 'src/crest_eventset_train.py', 'discover-thresholds', '--sites', 'config/sites_with_usgs.csv', '--output', 'config/sites_with_usgs.csv']
          download = ['python', 'src/nwps_multigage_model.py', 'download', '--sites', 'config/sites_with_usgs.csv', '--years', years] + force_arg
          train = ['python', 'src/crest_eventset_train.py', 'train', '--sites', 'config/sites_with_usgs.csv', '--event-set', event_set] + crest_arg

          if mode == 'smoke':
              run(discover)
              run(thresholds)
              run(with_limit(['python', 'src/nwps_multigage_model.py', 'download', '--sites', 'config/sites_with_usgs.csv', '--years', 1] + force_arg))
              run(with_limit(train))
              run(['python', 'src/nwps_multigage_model.py', 'status', '--sites', 'config/sites_with_usgs.csv'])
          elif mode == 'discover':
              run(discover)
          elif mode == 'thresholds':
              run(discover)
              run(thresholds)
          elif mode == 'download':
              run(discover)
              run(thresholds)
              run(with_limit(download))
          elif mode == 'train':
              run(discover)
              run(thresholds)
              run(with_limit(download))
              run(with_limit(train))
          elif mode == 'run-all':
              run(discover)
              run(thresholds)
              run(with_limit(download))
              run(with_limit(train))
          elif mode == 'status':
              site_file = 'config/sites_with_usgs.csv' if os.path.exists('config/sites_with_usgs.csv') else 'config/sites.csv'
              run(['python', 'src/nwps_multigage_model.py', 'status', '--sites', site_file])
          else:
              raise SystemExit(f'Unknown mode: {mode}')
          PY

      - name: Upload outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: river-model-output
          path: |
            config/sites_with_usgs.csv
            data/processed/
            output/
          if-no-files-found: ignore
          retention-days: 14
